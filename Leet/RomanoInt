/**
 * @param {string} s
 * @return {number}
 */
var romanToInt = function (s) {
  const conversion = {
    I: 1,
    V: 5,
    X: 10,
    L: 50,
    C: 100,
    D: 500,
    M: 1000,
  };

  let ans = 0;
  for (i = 0; i < s.length; i++) {
    if (conversion[s[i]] >= conversion[s[i + 1]] || s[i + 1] == undefined)
      ans += conversion[s[i]];
    else ans -= conversion[s[i]];
  }
  return ans;
};

console.log(romanToInt('LII')); //52
console.log(romanToInt('XCIV')); //94
